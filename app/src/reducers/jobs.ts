import { createSlice } from '@reduxjs/toolkit';

const jobs = createSlice({
    name: 'jobs',
    initialState: {},
    reducers: {
        renderJobs: function () {

        }
    }
})

export default jobs;
